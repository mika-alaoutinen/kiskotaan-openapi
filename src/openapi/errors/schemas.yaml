ErrorBody:
  type: object
  description: An error resource with an error code, a message and a HTTP status.
  required:
    - code
    - message
    - status
  properties:
    code:
      type: string
    message:
      type: string
    status:
      type: number
