NewCourse:
  required: true
  content:
    application/json:
      schema:
        $ref: "./schemas.yaml#/NewCourse"

NewPlayer:
  required: true
  content:
    application/json:
      schema:
        $ref: "./schemas.yaml#/NewPlayer"