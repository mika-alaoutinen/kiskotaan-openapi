NewCourse:
  required: true
  content:
    application/json:
      schema:
        $ref: "./schemas.yaml#/NewCourseResource"

NewPlayer:
  required: true
  content:
    application/json:
      schema:
        $ref: "./schemas.yaml#/NewPlayerResource"

NewScoreCard:
  required: true
  content:
    application/json:
      schema:
        $ref: "./schemas.yaml#/NewScoreCardResource"

ScoreCardId:
  required: true
  content:
    application/json:
      schema:
        $ref: "./schemas.yaml#/ScoreCardIdResource"

Scores:
  required: true
  content:
    application/json:
      schema:
        $ref: "./schemas.yaml#/ScoreRowResource"